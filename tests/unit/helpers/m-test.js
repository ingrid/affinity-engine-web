import { m } from 'affinity-engine-web/helpers/m';
import { module, test } from 'qunit';

module('Unit | Helper | m');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = m([42]);
  assert.ok(result);
});
