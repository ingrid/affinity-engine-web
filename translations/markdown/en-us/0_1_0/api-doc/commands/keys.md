Change the keys that the directable responds to.

* `keys` (**Object**):
  * `accept` (**Array**)
  * `escape` (**Array**)
  * `up` (**Array**)
  * `right` (**Array**)
  * `down` (**Array**)
  * `left` (**Array**)
